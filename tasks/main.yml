---
# Where the rubber meets the road. stuff gets done
- name: Manage ssh_known_hosts file
  action: sshknownhosts host={{ host }} state={{ state }} dest={{ dest }} enctype={{ enctype }} port={{ port }} keyscan={{ keyscan }} aliases={{ aliases }}
