---
galaxy_info:
  role_name: ssh_known_hosts
  author: Byron F. Martin
  description: This is an ansible role that manages the SSH known hosts file usually located at /etc/ssh/ssh_known_hosts or ~user/.ssh/known_hosts.
  license: BSD
  min_ansible_version: 2.0
  platforms:
    - name: GenericBSD
      versions:
        - all
    - name: GenericLinux
      versions:
        - all
  galaxy_tags:
    - ssh
    - networking
    - system
dependencies: []
